# herd-store-duckdb — StoreAdapter for DuckDB
# These vars are set in herd-core's .env (HDR-0030: config rolls up to core).
# This file is for reference only.
#
# Naming convention: HERD_<PROTOCOL>_<ADAPTER>_<VAR> (HDR-0032)

HERD_STORE_DUCKDB_PATH=.herd/herddb.duckdb   # Database file path (default: .herd/herddb.duckdb)
